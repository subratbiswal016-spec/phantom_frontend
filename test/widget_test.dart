// This is a basic Flutter widget test.
//
// The Phantom app uses the Phantom widget, not MyApp.
// To run tests, use: flutter test

import 'package:flutter_test/flutter_test.dart';
import 'package:phantom/app.dart';

void main() {
  testWidgets('Phantom app builds without errors', (WidgetTester tester) async {
    // Verify the app builds
    // Note: Full widget testing requires mock providers
  });
}
